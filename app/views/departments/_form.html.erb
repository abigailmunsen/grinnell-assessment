<%= form_with(model: department, local: true) do |form| %>
  <% if department.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(department.errors.count, "error") %> prohibited this department from being saved:</h2>

      <ul>
      <% department.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :deptSubject %>
    <%= form.text_field :deptSubject %>
  </div>

  <div class="field">
    <%= form.label :deptFullName %>
    <%= form.text_field :deptFullName %>
  </div>

  <div class="field">
    <%= form.label :deptCode %>
    <%= form.text_field :deptCode %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
